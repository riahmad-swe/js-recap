//! Tax calculation
// const getTax = (amount, taxRate) => (amount * taxRate) / 100;

// const add = (p, q) => p + q;

//! single parameter use
// const getSquare = (x) => x * x;
// const getHalf = (num) => num / 2;
// console.log(getSquare(9));
// console.log(getHalf(20));

//! array declaration
const firstElement = (nums) => nums[0];
console.log(firstElement([345, 34, 534, 45, 85]));

//! annonymous function
// () => {

// }
